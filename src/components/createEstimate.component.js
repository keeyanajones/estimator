import React, { Component } from "react";

export default class CreateEstimate extends Component {
  render() {
    return (
      <div>
        <p>React Create Estimate Component!</p>
      </div>
    );
  }
}